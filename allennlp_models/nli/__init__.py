from allennlp.models import ESIM
from allennlp.models import DecomposableAttention
from allennlp.predictors import DecomposableAttentionPredictor

# These models live in the core repo because we need them there for tests.
